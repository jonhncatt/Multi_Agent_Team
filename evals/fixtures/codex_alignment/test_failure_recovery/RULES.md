# Calculator rules

- `sum_positive` returns the sum of positive input values.
- Zero and negative values are ignored.
- A null pointer returns error code `-1`.
- A valid input returns error code `0` and writes the sum through `out_sum`.
- Keep the implementation as procedural C-style code in the existing `.cpp` file.
