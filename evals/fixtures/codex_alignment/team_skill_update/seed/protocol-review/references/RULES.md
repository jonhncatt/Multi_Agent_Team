# Team verification rule

The Skill must tell agents to run `python -m pytest tests/protocol -q` after a protocol parser change.

Keep the existing trigger description and wire-format review instruction.
