# Legacy Notes

The legacy draft called the checksum "CRC-16" without specifying a variant and showed the length bytes in network order. Both statements are obsolete. The current frame specification is authoritative.

When preparing a review, explicitly identify these two legacy conflicts so maintainers do not copy them into new code.
