# Documentation Rules

Update only `GUIDE.md`. The guide must contain sections named `Build`, `Validate`, `Failure handling`, and `Compatibility`. Use commands exactly as documented in `REFERENCE.md`; do not invent package installation steps.

The guide must state that failed validation means the task remains incomplete.
