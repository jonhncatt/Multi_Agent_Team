# Change Notes

The parser now rejects oversized frames before reading payload bytes. On validation failure, capture the failing test name and the first relevant error line. Do not claim completion and do not recommend deleting the build directory as the first response.
