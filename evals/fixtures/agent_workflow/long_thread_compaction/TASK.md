# Handoff task

Create `HANDOFF.md` from the decisions retained in the compressed Thread context.

The handoff must list the release marker, Windows compiler choice, and ABI constraint. Do not invent values when the compressed Thread already supplies them.
