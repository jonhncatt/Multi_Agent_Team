# Draft split produced by the OSS dependency audit.
# Office/document parsing features are useful, but not required for the minimum runtime.

pypdf>=5.0.0
pdfplumber>=0.11.4
python-docx>=1.1.2
openpyxl>=3.1.5
