interface:
  display_name: "Update Task"
  short_description: "Draft complete Task updates for human approval"
  default_prompt: "Use $update-task to resolve the intended existing Task, draft a complete update, and request approval before saving."
